<button {{on "click" (fn (mut this.show) true)}} button thinner ...attributes>
	{{#if (has-block)}}
		{{yield}}
	{{else if @text}}
		{{@text}}
	{{else}}
		<i class="fal fa-envelope"></i>
		<span>Sign up for our newsletter</span>
	{{/if}}
</button>

{{#if this.show}}

	<Ascua::Modal @onClose={{fn (mut this.show) false}}>

		<Modal::Close @close={{fn (mut this.show) false}} />

		<script>
			hbspt.forms.create({
				region: "na1",
				portalId: "44363239",
				formId: "91f5b2b7-783d-4027-885e-ff17e133a000"
			});
		</script>

	</Ascua::Modal>

{{/if}}